import 'package:flutter/material.dart';

/// Fallback palette for the public portfolio. Runtime values still come from
/// the Colors feature and the admin dashboard.
abstract final class AppColors {
  static const scaffoldBackground = Color(0xFF0A192F);
  static const surface = Color(0xFF112240);
  static const primaryText = Color(0xFFCCD6F6);
  static const secondaryText = Color(0xFF8892B0);
  static const accent = Color(0xFF64FFDA);
  static const border = Color(0xFF233554);
  static const error = Color(0xFFFF6B81);
  static const success = Color(0xFF64FFDA);
}
